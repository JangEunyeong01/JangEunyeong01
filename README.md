# Fitto

숫자 대신 캐릭터 표정으로 건강 상태를 알려주는 건강관리 앱.

물을 얼마나 마셨는지, 오늘 얼마나 움직였는지를 숫자로만 보여주면 잘 안 보게 됩니다.
Fitto는 물방울 캐릭터 "피또"의 표정이 5단계로 변하는 걸 먼저 보여주고, 숫자는 그 다음에 둡니다.

> Expo + React Native + TypeScript로 만든 개인 프로젝트입니다.
> 디자인 핸드오프 문서의 명세(색·타이포·모션 값)를 그대로 구현하는 것을 목표로 했습니다.

## 스크린샷

<!-- 온보딩 / 홈 / 식단 / 헬스 순으로 추가 예정 -->

| 온보딩 | 홈 | 식단 | 헬스 |
| --- | --- | --- | --- |
|  |  |  |  |

## 주요 기능

**온보딩 9단계** — 기본 정보 → 활동량 → 목표 → 건강 상태 → 식단 취향 → 못 먹는 음식 → 피또 성격 → 완료.
입력값으로 Mifflin-St Jeor 공식을 써서 BMR·TDEE를 계산하고 목표 칼로리와 물 목표를 잡습니다.
완료 화면에서 계산 근거(성별·나이·키·몸무게)를 함께 보여줘 숫자를 검증할 수 있습니다.

**홈 대시보드** — 칼로리·물·걸음수·활동·주간 요약 카드. 카드 순서를 바꾸거나 숨길 수 있습니다
(칼로리·걸음수는 필수라 숨길 수 없습니다). 물 카드는 컵을 탭하거나 세로로 드래그해 증감합니다.

**식단** — 끼니별 기록, 음식 검색 바텀시트, 알레르기 자동 필터링, 주간 영양 밸런스 분석,
직접 재료를 넣어 만드는 레시피.

**헬스** — 움직임 현황, 규칙 기반 운동 추천, 운동 기록.

**생리 주기** — 캘린더에 생리·가임기·배란일 표시, 날짜별 컨디션·증상 기록. 설정에서 끌 수 있습니다.

**그 외** — 시간대별 배경 그라데이션(새벽~밤 6단계), 피또 말투 3종(친근형·엄격형·중립형),
라이트/다크 모드, 생일 당일 축하 배너, 첫 실행 튜토리얼.

## 기술 스택

| | |
| --- | --- |
| 프레임워크 | Expo SDK 57, React Native 0.86, React 19 |
| 언어 | TypeScript (strict) |
| 내비게이션 | React Navigation — 바텀탭 + 탭별 네이티브 스택 |
| 상태 관리 | Zustand + AsyncStorage 영속화 (버전 마이그레이션 포함) |
| 애니메이션 | React Native `Animated` |
| 그래픽 | react-native-svg, expo-linear-gradient, expo-blur |
| 폰트 | Pretendard (OFL) |

## 실행

```bash
npm install
npm start
```

Expo Go 앱으로 QR을 찍거나, `npm run android` · `npm run ios` · `npm run web`으로 실행합니다.

## 구조

```
src/
├─ components/   버튼·칩·배지·입력 등 공통 UI (14개)
├─ screens/      화면별 컴포넌트
├─ navigation/   탭·스택 구성, 커스텀 탭바
├─ store/        Zustand 스토어 (앱 상태 + 시트/모달 제어)
├─ theme/        디자인 토큰, 폰트, 테마 훅
├─ utils/        목표 계산·주기 계산·영양 분석 등 순수 로직
├─ data/         음식·재료·운동 목데이터
└─ copy/         피또 말투별 문구
```

## 구현하면서 신경 쓴 것

**디자인 토큰을 한 곳에 모았습니다.** 처음에는 화면마다 `fontSize`를 직접 적어서
같은 역할인데 11 / 11.5 / 12 / 12.5px이 뒤섞였습니다. 역할 기반 타이포 토큰
(`sectionTitle`, `bodySm`, `badge` …)으로 바꾸면서 하드코딩된 값 230개를 25개로 줄였습니다.
입력 필드도 높이가 50/46/44/42px로 흩어져 있던 것을 두 가지 크기로 통일했습니다.

**표면 색을 용도에 따라 나눴습니다.** 유리 카드용 색(`card`/`stroke`)을 카드 *안에서* 쓰면
같은 흰색이라 입력창이 통째로 사라집니다. 배경 위(`onBackground`)와 카드 안을 구분해
컴포넌트가 알아서 고르게 했습니다.

**웹에서만 나는 문제들을 처리했습니다.** `<input>`은 `min-width: auto`라 `flex: 1`을 줘도
기본 너비 밑으로 안 줄어들어 한 줄에 여러 개를 놓으면 부모를 넘어갑니다.
Pretendard는 굵기별로 파일이 나뉘어 있어서 `fontWeight`만 바꾸면 두께가 안 변하고
`fontFamily`를 함께 바꿔야 합니다.

**목데이터도 그럴듯하게 보이도록** 시드 기반으로 만들었습니다. 처음엔 해시가 약해서
모든 주가 비슷한 값(12.6천)으로 나왔고, FNV-1a + murmur3 믹싱으로 바꿔 해결했습니다.

## 알려진 제약

- 걸음수·앉은 시간은 기기 센서 대신 목데이터입니다. HealthKit / Google Fit 연동이 다음 단계입니다.
- 영양 분석은 규칙 기반입니다. 식약처 식품영양성분 API 연동 예정입니다.
- 사진 첨부, 체중 기록, 글씨 크기 조절은 자리만 잡아둔 상태입니다.

## 라이선스

[MIT](LICENSE). 번들된 Pretendard 폰트는 SIL Open Font License 1.1을 따릅니다
(`assets/fonts/Pretendard-LICENSE.txt`).
